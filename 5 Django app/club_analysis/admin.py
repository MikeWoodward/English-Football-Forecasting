"""
Admin configuration for the club_analysis app.
"""
from django.contrib import admin

# Register your models here.
# Note: Models are registered in the goals app admin since they share
# the same database models (ClubHistory, ClubSeason, League, FootballMatch).
