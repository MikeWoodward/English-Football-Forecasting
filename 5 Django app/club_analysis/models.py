"""
Models for the club_analysis app.
"""
from django.db import models

# Create your models here.
# Note: This app uses models defined in the goals app
# (ClubHistory, ClubSeason, League, FootballMatch) rather than
# defining its own models.
